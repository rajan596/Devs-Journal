

## Postgres Queries

## Storage

- Postgres stores data in **B Tree**

## Postgres Triggers and LISTEN/NOTIFY
- A trigger is a special function in a database that you can set up to run automatically whenever a specific action occurs on a specific table.
- The trigger broadcasts a NOTIFY message on the configured channel
- We can LISTEN on this channel 
